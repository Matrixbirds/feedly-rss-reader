import React from 'react'
import Layout from './layout';

export default class Saved extends React.Component {
    render () {
        return (
            <Layout>
                <h2>saved</h2>
            </Layout>
        )
    }
}
